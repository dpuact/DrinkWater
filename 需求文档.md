# 《喝水记录 PWA 软件开发需求文档》

**项目名称：** 喝水记录
**项目类型：** 移动端优先 PWA（Progressive Web App）
**项目版本：** V1.0
**目标平台：** 手机浏览器 + Android/iOS PWA
**主要语言：** 中文
**核心目标：** 让用户能够以极低的操作成本记录每天喝水的次数和饮水量，并提供简单、直观的统计分析。

---

# 一、项目概述

## 1.1 项目背景

开发一款非常简单、轻量的个人喝水记录软件。

软件不追求复杂的健康管理功能，而是专注于一个核心需求：

> **用户喝了一杯水之后，用 1～3 秒完成记录。**

用户可以记录：

* 喝水时间
* 本次饮水量
* 每天喝水次数
* 每天总饮水量
* 每日饮水目标
* 最近几天/几周的饮水统计

软件应该具有类似手机 App 的使用体验，但第一阶段不开发原生 Android/iOS App，而是采用 PWA 技术。

---

# 二、产品定位

## 2.1 核心定位

这是一个：

> **极简、快速、无负担的个人喝水记录工具。**

而不是：

* 医疗软件
* 健康诊断软件
* 复杂运动管理软件
* 饮食管理软件
* 社交软件

---

# 三、核心设计原则

整个项目必须遵循以下原则。

## 3.1 快

记录一次喝水最好只需要：

**点击一次按钮。**

例如用户喝了一杯 250ml 的水：

打开软件 → 点击「+250ml」

完成。

---

## 3.2 简

首页不能出现大量复杂功能。

用户打开软件后，第一眼应该看到：

1. 今天喝了多少
2. 今天目标是多少
3. 当前完成百分比
4. 快速添加饮水按钮
5. 今天的记录

---

## 3.3 手机优先

整个项目必须采用 Mobile First 设计。

优先考虑：

* 手机竖屏
* 单手操作
* 大按钮
* 大字体
* 触摸操作
* 底部导航
* 安全区域
* iPhone 刘海/底部 Home Indicator
* Android 不同屏幕尺寸

电脑端只是兼容，而不是主要使用场景。

---

## 3.4 不强制登录

V1.0：

**不要求用户注册账号。**

打开即可使用。

数据优先保存在本机浏览器数据库。

---

# 四、PWA 要求

项目必须按照标准 PWA 方式开发。

需要包含：

* Web App Manifest
* PWA 图标
* Service Worker
* HTTPS 部署
* 离线缓存
* 添加到手机主屏幕
* standalone 独立窗口运行

Manifest 至少应正确配置：

* name
* short_name
* start_url
* display
* icons

其中应提供至少 192×192 和 512×512 的应用图标，以满足常见 Chromium PWA 安装要求。

建议：

```text
display: standalone
```

安装后让软件尽量表现得像普通手机 App，而不是浏览器网页。PWA 安装后可以拥有独立图标和独立窗口。

---

# 五、推荐技术架构

## 5.1 前端

推荐：

```text
React
+
TypeScript
+
Vite
```

---

## 5.2 UI

推荐：

```text
Tailwind CSS
```

如果需要组件库，可以使用：

```text
shadcn/ui
```

但是：

**不要为了这个项目引入过于庞大的 UI 框架。**

---

## 5.3 图表

统计页面使用：

```text
ECharts
```

或者其他轻量图表库。

---

## 5.4 本地数据库

V1.0 使用：

```text
IndexedDB
```

推荐使用：

```text
Dexie.js
```

作为 IndexedDB 的封装。

不要把大量饮水记录全部塞进 LocalStorage。

---

## 5.5 后端

V1.0：

**不需要后端。**

数据全部保存在用户设备。

---

## 5.6 V2.0

未来如果需要跨设备同步，再增加：

```text
用户账号
+
云端数据库
+
API
```

可以考虑：

```text
Supabase
```

或者：

```text
Node.js + PostgreSQL
```

但 V1.0 不实现。

---

# 六、页面结构

整个软件第一版建议只设计 4 个主要页面。

```text
首页
统计
记录
设置
```

底部固定导航：

```text
┌─────────────────────────────┐
│                             │
│          页面内容            │
│                             │
├─────────────────────────────┤
│   🏠首页   📊统计   📋记录   ⚙设置 │
└─────────────────────────────┘
```

---

# 七、首页

首页是整个软件最重要的页面。

## 7.1 页面目标

用户打开 App 后：

**3 秒内完成一次饮水记录。**

---

# 八、首页顶部

显示当前日期。

例如：

```text
8月20日 星期四
```

下面显示：

```text
今天喝水
```

---

# 九、核心饮水量显示

页面中央显示：

```text
1,250
ml
```

下面：

```text
目标 2,000 ml
```

再显示：

```text
62%
```

---

# 十、进度条

显示今日目标完成情况。

例如：

```text
████████████░░░░░░░░
```

计算：

```text
完成百分比 = 今日饮水量 / 每日目标 × 100%
```

但是最大显示限制为：

```text
100%
```

即：

如果目标 2000ml：

喝了 2500ml：

页面显示：

```text
100%
```

同时可以显示：

```text
超出目标 500ml
```

---

# 十一、今日次数

显示：

```text
今日 5 次
```

---

# 十二、快速饮水按钮

首页必须提供最核心的快速添加区域。

推荐：

```text
┌────────┐ ┌────────┐ ┌────────┐
│ +100ml │ │ +150ml │ │ +200ml │
└────────┘ └────────┘ └────────┘

┌────────┐ ┌────────┐ ┌────────┐
│ +250ml │ │ +300ml │ │ +500ml │
└────────┘ └────────┘ └────────┘
```

点击之后立即记录。

例如：

```text
点击 +250ml
↓
记录当前时间
↓
增加今日饮水量 250ml
↓
次数 +1
↓
刷新首页
```

不需要弹窗确认。

---

# 十三、默认快捷饮水量

默认提供：

```text
100ml
150ml
200ml
250ml
300ml
500ml
```

用户可以在设置中修改。

---

# 十四、自定义饮水量

首页应该提供：

```text
＋ 自定义
```

点击后出现输入框：

```text
请输入饮水量

[       ] ml

取消       确定
```

允许输入：

```text
1～5000ml
```

不允许：

* 负数
* 0
* 字母
* 非法字符

---

# 十五、饮水记录列表

首页下方显示当天记录。

例如：

```text
今日记录

16:20                  250 ml
15:05                  300 ml
13:40                  250 ml
11:25                  200 ml
09:10                  250 ml
```

最新记录显示在最上面。

---

# 十六、单条记录操作

点击某条记录，可以打开详情。

例如：

```text
饮水记录

时间
2026-08-20 16:20

饮水量
250 ml
```

提供：

```text
修改
删除
```

---

# 十七、删除记录

点击删除后：

```text
确定删除这条饮水记录？

250 ml
16:20

取消      删除
```

删除之后：

* 今日总量减少
* 今日次数减少
* 统计数据同步更新

---

# 十八、修改记录

用户可以修改：

```text
饮水时间
饮水量
```

例如：

```text
时间：

2026-08-20
16:20

饮水量：

250 ml
```

修改保存后重新计算统计。

---

# 十九、统计页面

统计页面用于查看长期饮水情况。

分为：

```text
今日
7天
30天
```

---

# 二十、今日统计

显示：

```text
今日饮水

总量
1,750 ml

次数
6 次

目标
2,000 ml

完成度
87.5%
```

---

# 二十一、7天统计

显示最近 7 天。

例如：

```text
周四  1750 ml
周三  2100 ml
周二  1850 ml
周一  2200 ml
周日  1600 ml
周六  1950 ml
周五  2050 ml
```

使用柱状图。

X轴：

```text
日期
```

Y轴：

```text
ml
```

同时显示每日目标线。

---

# 二十二、30天统计

显示最近 30 天饮水趋势。

包括：

* 每日饮水量
* 每日目标
* 平均饮水量
* 达标天数
* 达标率
* 总饮水量

---

# 二十三、统计指标

## 平均每日饮水量

```text
总饮水量 / 有记录的天数
```

---

## 达标天数

例如：

30天中：

```text
达到每日目标：22天
```

---

## 达标率

```text
达标天数 / 统计天数 × 100%
```

---

## 最大饮水量

例如：

```text
最高：2,850 ml
```

---

## 最少饮水量

例如：

```text
最低：950 ml
```

---

# 二十四、记录页面

记录页面用于查看完整历史记录。

可以按照日期分组。

例如：

```text
2026年8月20日
总计 1750ml · 6次

16:20   250ml
15:05   300ml
13:40   250ml
12:10   300ml
10:30   250ml
08:50   400ml


2026年8月19日
总计 2100ml · 7次
```

---

# 二十五、记录筛选

可以按照：

```text
今天
昨天
最近7天
最近30天
自定义日期
```

查看。

---

# 二十六、设置页面

设置页面包含：

```text
每日饮水目标
快捷饮水量
提醒设置
数据管理
主题
关于
```

---

# 二十七、每日饮水目标

默认：

```text
2000 ml
```

用户可以修改。

例如：

```text
每日目标

[ 2000 ] ml
```

建议允许：

```text
500～10000ml
```

---

# 二十八、快捷饮水量设置

用户可以自定义首页按钮。

默认：

```text
100
150
200
250
300
500
```

例如用户可以改成：

```text
150
250
300
350
500
750
```

最多建议设置 6 个快捷按钮。

---

# 二十九、提醒功能

V1.0 可以设计，但建议作为可选功能。

用户打开：

```text
喝水提醒
```

可以设置：

```text
开启 / 关闭
```

---

# 三十、提醒时间

提供：

```text
开始时间
结束时间
提醒间隔
```

例如：

```text
开始：09:00
结束：21:00
间隔：90分钟
```

系统根据设置产生提醒。

---

# 三十一、提醒文案

例如：

```text
💧 该喝水啦

别忘了喝一杯水。
```

或者：

```text
💧 喝水时间

今天已经喝了 1250ml。
```

---

# 三十二、提醒功能的技术要求

提醒功能必须考虑浏览器和操作系统差异。

不能假设：

> 所有手机都能像原生 App 一样稳定地进行本地定时通知。

因此 V1.0 应将提醒功能设计成**渐进增强功能**。

如果当前设备/浏览器支持，则启用。

如果不支持：

显示：

```text
当前设备不支持完整的后台喝水提醒。
```

不要导致整个软件无法使用。

---

# 三十三、数据结构

## WaterRecord

每次喝水创建一条记录。

字段：

```text
id
amount
timestamp
date
createdAt
updatedAt
```

示例：

```json
{
  "id": "uuid",
  "amount": 250,
  "timestamp": "2026-08-20T16:20:00+08:00",
  "date": "2026-08-20",
  "createdAt": "2026-08-20T16:20:00+08:00",
  "updatedAt": "2026-08-20T16:20:00+08:00"
}
```

---

# 三十四、UserSettings

用户设置：

```text
dailyGoal
quickAmounts
reminderEnabled
reminderStartTime
reminderEndTime
reminderInterval
theme
```

示例：

```json
{
  "dailyGoal": 2000,
  "quickAmounts": [100, 150, 200, 250, 300, 500],
  "reminderEnabled": false,
  "reminderStartTime": "09:00",
  "reminderEndTime": "21:00",
  "reminderInterval": 90,
  "theme": "system"
}
```

---

# 三十五、日期处理

必须统一日期处理逻辑。

重要要求：

**以用户设备当前时区为准。**

不能简单使用服务器 UTC 日期。

例如用户在中国：

```text
2026-08-20 23:50
```

应该属于：

```text
2026-08-20
```

到了：

```text
2026-08-21 00:01
```

才属于下一天。

---

# 三十六、每日统计计算

不要单独保存：

```text
todayTotal
todayCount
```

作为主要数据来源。

应该根据 WaterRecord 实时计算。

例如：

```text
今日所有记录
↓
SUM(amount)
↓
今日总饮水量
```

次数：

```text
COUNT(records)
```

这样可以避免修改/删除记录后数据不一致。

---

# 三十七、超过目标

例如：

目标：

```text
2000ml
```

实际：

```text
2300ml
```

显示：

```text
2,300 ml

目标 2,000 ml

已完成 100%
超出 300 ml
```

---

# 三十八、零记录状态

第一次打开软件时：

```text
今天还没有喝水记录

💧

开始记录你的第一杯水吧
```

同时显示快捷按钮。

---

# 三十九、一天结束状态

如果当天没有记录：

统计页面应该显示：

```text
暂无饮水记录
```

不能显示错误数据。

---

# 四十、空数据处理

所有页面必须考虑：

* 没有记录
* 只有一天记录
* 没有最近7天数据
* 没有最近30天数据
* 删除全部记录
* 第一次使用

不能出现：

```text
NaN
undefined
Infinity
```

---

# 四十一、数据导出

V1.0 建议加入。

设置 → 数据管理：

```text
导出数据
```

导出：

```text
JSON
CSV
```

---

# 四十二、JSON 导出

例如：

```json
{
  "version": 1,
  "exportDate": "2026-08-20",
  "settings": {},
  "records": []
}
```

---

# 四十三、CSV 导出

字段：

```text
日期,时间,饮水量
2026-08-20,09:10,250
2026-08-20,11:20,300
2026-08-20,14:30,250
```

---

# 四十四、数据导入

设置中提供：

```text
导入数据
```

允许选择之前导出的 JSON 文件。

导入之前必须提示：

```text
导入数据会将记录合并到当前设备。

是否继续？
```

建议默认采用：

**合并数据。**

而不是直接覆盖。

---

# 四十五、数据清空

设置：

```text
清空所有数据
```

必须进行二次确认。

第一次：

```text
清空所有数据
```

第二次：

```text
此操作无法恢复。

将删除所有饮水记录和设置。

确定删除？
```

必须防止误触。

---

# 四十六、主题

支持：

```text
跟随系统
浅色
深色
```

默认：

```text
跟随系统
```

---

# 四十七、UI视觉设计

整体风格：

**简洁、清爽、现代、健康。**

不要设计成医院软件。

不要使用大量渐变。

不要使用复杂动画。

---

# 四十八、颜色建议

主色：

```text
蓝色 / 水蓝色
```

背景：

```text
浅灰 / 白色
```

深色模式：

```text
深灰背景
```

重点：

```text
饮水量
```

应该成为视觉中心。

---

# 四十九、按钮设计

所有主要按钮必须适合手指触摸。

不要使用很小的文字链接作为主要操作。

推荐：

```text
高度 48～56px
```

主要操作按钮尽量：

```text
≥ 48px
```

---

# 五十、动画

可以增加非常轻微的反馈：

点击：

```text
+250ml
```

之后：

```text
+250ml
```

按钮轻微缩放。

数字：

```text
1250
↓
1500
```

可以有短暂动画。

但动画必须：

**短、快、不影响操作。**

---

# 五十一、首页推荐布局

手机屏幕：

```text
┌──────────────────────────┐
│  8月20日 星期四           │
│                          │
│       今天喝水            │
│                          │
│       1,250              │
│          ml              │
│                          │
│      目标 2,000 ml        │
│                          │
│  ████████████░░░░░░      │
│                          │
│       今日 5 次           │
│                          │
│ ┌──────┐ ┌──────┐        │
│ │+100ml│ │+150ml│        │
│ └──────┘ └──────┘        │
│                          │
│ ┌──────┐ ┌──────┐        │
│ │+250ml│ │+300ml│        │
│ └──────┘ └──────┘        │
│                          │
│       ＋ 自定义           │
│                          │
│ 今日记录                 │
│                          │
│ 16:20          250 ml    │
│ 15:05          300 ml    │
│ 13:40          250 ml    │
│                          │
├──────────────────────────┤
│ 首页   统计   记录   设置 │
└──────────────────────────┘
```

---

# 五十二、响应式设计

必须支持：

```text
320px
375px
390px
414px
430px
768px
1024px
1440px
```

手机端优先。

---

# 五十三、底部导航

手机端固定底部。

四个入口：

```text
首页
统计
记录
设置
```

当前页面需要有明显的 active 状态。

同时考虑：

```text
env(safe-area-inset-bottom)
```

避免 iPhone 底部 Home Indicator 遮挡按钮。

---

# 五十四、桌面端

电脑打开时：

不要让页面无限拉伸。

例如：

```text
max-width: 480px
```

让整个 App 保持手机 App 的视觉比例。

也可以在桌面端增加轻微背景。

---

# 五十五、离线功能

PWA 必须支持基本离线使用。

用户至少应该能够：

* 打开软件
* 查看历史数据
* 添加饮水记录
* 修改记录
* 删除记录
* 查看统计

即使暂时没有网络，也不能影响核心记录功能。

Service Worker 可以用于缓存应用资源，使应用具备离线体验。

---

# 五十六、PWA 文件

项目至少包含：

```text
manifest.webmanifest
service-worker
icons/
```

建议图标：

```text
icon-192.png
icon-512.png
icon-maskable-512.png
```

Manifest 示例结构：

```json
{
  "name": "喝水记录",
  "short_name": "喝水",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#3b82f6",
  "icons": [
    {
      "src": "/icons/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/icons/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

Manifest 是浏览器安装 PWA 时描述应用名称、图标、启动方式等信息的标准文件。

---

# 五十七、PWA 安装提示

首页可以根据设备状态显示：

```text
📱 把「喝水」添加到手机桌面

以后打开手机就能快速记录喝水。

[添加到主屏幕]
```

但是：

**不能每次打开都弹。**

只在合适的时候提示。

如果当前浏览器不支持程序化安装：

显示：

```text
点击浏览器菜单
→ 添加到主屏幕
```

---

# 五十八、安全

V1.0 不处理：

* 身份证
* 手机号
* 地址
* 医疗数据
* 支付信息

因此不需要账号系统。

但仍然需要：

* HTTPS
* 不加载不必要的第三方脚本
* 不上传用户饮水数据
* 防止 XSS
* 输入数据校验

---

# 五十九、隐私原则

V1.0：

**默认不上传饮水数据。**

用户数据存储在本机。

页面可以明确显示：

```text
你的饮水记录默认只保存在当前设备。
```

---

# 六十、数据丢失说明

因为 V1.0 使用本地存储：

用户：

* 清除浏览器数据
* 卸载 PWA
* 更换设备

可能导致本地数据丢失。

因此设置页面必须提供：

```text
导出数据
```

并提示：

```text
建议定期导出数据进行备份。
```

---

# 六十一、未来云同步

V2.0 可以加入：

```text
登录
```

然后：

```text
手机
   ↓
云端
   ↓
电脑
   ↓
另一台手机
```

实现跨设备同步。

但 V1.0 不实现。

---

# 六十二、错误处理

任何数据库操作失败：

不能白屏。

显示：

```text
操作失败，请重试。
```

并在开发环境记录错误日志。

---

# 六十三、重复点击防护

用户快速连续点击：

```text
+250ml
+250ml
+250ml
```

应该正确记录三次：

```text
250
250
250
```

不能因为按钮防抖导致漏记录。

可以使用短时间 UI 防重复机制，但不能阻止用户正常连续记录。

---

# 六十四、撤销功能

建议加入：

用户点击：

```text
+250ml
```

页面底部短暂显示：

```text
已记录 250ml

撤销
```

用户误点时可以：

```text
撤销
```

恢复之前的数据。

撤销窗口建议：

```text
3～5秒
```

---

# 六十五、首次使用

第一次打开：

可以简单显示：

```text
💧 喝水记录

简单记录每一次喝水。

每天喝多少，
一目了然。

[开始使用]
```

然后进入设置每日目标：

```text
你的每日饮水目标

[2000] ml

[开始记录]
```

但：

**不要强制复杂的新手教程。**

---

# 六十六、默认值

第一次使用默认：

```text
每日目标：2000ml
```

快捷按钮：

```text
100ml
150ml
200ml
250ml
300ml
500ml
```

提醒：

```text
关闭
```

主题：

```text
跟随系统
```

---

# 六十七、统计计算规则

所有统计必须基于 WaterRecord 计算。

## 今日总量

```text
SUM(amount)
WHERE date = 今天
```

## 今日次数

```text
COUNT(records)
WHERE date = 今天
```

## 7天总量

统计今天以及之前 6 天。

## 30天总量

统计今天以及之前 29 天。

---

# 六十八、数据排序

记录默认：

```text
时间倒序
```

即：

最新 → 最旧。

---

# 六十九、时间精度

至少精确到：

```text
分钟
```

数据库可以保存完整：

```text
timestamp
```

例如：

```text
2026-08-20T16:20:35+08:00
```

UI显示：

```text
16:20
```

---

# 七十、未来功能预留

代码架构必须方便以后增加：

```text
饮水类型
每日提醒
云同步
账号
多设备同步
数据备份
桌面小组件
原生 App
Apple Health
Google Health Connect
```

但：

**V1.0 不实现这些功能。**

---

# 七十一、暂时不要做的功能

为了避免项目变得复杂，V1.0 明确禁止加入：

* 社交
* 好友
* 排行榜
* 分享
* AI健康建议
* 医疗建议
* BMI
* 减肥计划
* 运动计划
* 饮食管理
* 睡眠管理
* 广告
* 会员
* 支付
* 复杂账号体系

项目核心就是：

> **记录喝水。**

---

# 七十二、项目目录建议

推荐：

```text
src/
├── components/
│   ├── WaterProgress.tsx
│   ├── QuickAmountButtons.tsx
│   ├── WaterRecordItem.tsx
│   ├── BottomNavigation.tsx
│   └── ConfirmDialog.tsx
│
├── pages/
│   ├── Home.tsx
│   ├── Statistics.tsx
│   ├── Records.tsx
│   └── Settings.tsx
│
├── db/
│   ├── database.ts
│   └── waterRecords.ts
│
├── hooks/
│   ├── useWaterRecords.ts
│   ├── useTodayStats.ts
│   └── useSettings.ts
│
├── utils/
│   ├── date.ts
│   ├── statistics.ts
│   └── export.ts
│
├── types/
│   └── index.ts
│
├── components/
│
├── App.tsx
└── main.tsx
```

---

# 七十三、开发阶段

## Phase 1：项目基础

完成：

* React
* TypeScript
* Vite
* Tailwind
* 路由
* 基础布局

---

## Phase 2：数据库

完成：

* IndexedDB
* WaterRecord
* UserSettings
* CRUD

必须先确保数据逻辑正确。

---

## Phase 3：首页

完成：

* 今日饮水量
* 目标
* 进度
* 快捷按钮
* 自定义饮水
* 今日记录

---

## Phase 4：记录管理

完成：

* 修改
* 删除
* 查看历史
* 日期筛选

---

## Phase 5：统计

完成：

* 今日
* 7天
* 30天
* 图表
* 平均值
* 达标率

---

## Phase 6：设置

完成：

* 每日目标
* 快捷按钮
* 主题
* 数据导出
* 数据导入
* 清空数据

---

## Phase 7：PWA

完成：

* Manifest
* Icon
* Service Worker
* Cache
* Offline
* Install

---

## Phase 8：测试

完成：

* Android
* iPhone
* Chrome
* Safari
* Edge

重点测试：

* 安装
* 离线
* 数据保存
* 数据删除
* 日期切换
* 深色模式
* 小屏手机
* 大屏手机

---

# 七十四、验收标准

## 首页

打开首页后必须能够看到：

* 今日日期
* 今日饮水量
* 每日目标
* 完成百分比
* 今日次数
* 快速饮水按钮
* 今日记录

---

## 添加记录

点击：

```text
+250ml
```

必须立即：

```text
总量 +250ml
次数 +1
列表新增一条记录
```

---

## 删除

删除一条记录：

```text
总量正确减少
次数正确减少
统计同步更新
```

---

## 修改

修改：

```text
250ml → 500ml
```

统计必须同步：

```text
+250ml
```

---

## 日期

跨过：

```text
00:00
```

之后：

```text
今日数据重新计算
```

昨天数据不能混入今天。

---

## 刷新

浏览器刷新之后：

**数据不能丢失。**

---

## 离线

关闭网络：

仍然能够：

* 打开
* 查看
* 添加
* 修改
* 删除

---

## PWA

安装后：

* 桌面有图标
* 可以独立启动
* 不显示普通浏览器地址栏
* 页面正常加载
* 数据正常保存

---

# 七十五、性能要求

首页打开后应该尽可能快速。

目标：

```text
首屏 < 2秒
```

核心操作：

```text
点击按钮 → 数据更新
```

应该几乎立即完成。

不要因为统计图表、动画等影响首页操作。

---

# 七十六、代码质量

要求：

* TypeScript 严格类型
* 不允许大量 any
* 组件合理拆分
* 数据层与 UI 分离
* 统计计算与 UI 分离
* 所有核心函数有明确类型
* 避免重复代码
* 避免魔法数字
* 错误统一处理

---

# 七十七、重要架构原则

必须做到：

```text
UI
 ↓
Hooks
 ↓
Service
 ↓
Database
```

不要：

```text
React组件
 ↓
直接到处操作 IndexedDB
```

数据逻辑必须集中管理。

---

# 七十八、第一版最终产品结构

最终用户看到的就是：

```text
┌─────────────────────────┐
│                         │
│       喝水记录           │
│                         │
│       1750 ml           │
│       / 2000 ml         │
│                         │
│    █████████████░░      │
│                         │
│       今日 6 次          │
│                         │
│  +100  +150  +200       │
│  +250  +300  +500       │
│                         │
│       ＋ 自定义          │
│                         │
│  今日记录                │
│                         │
│  16:20       250ml      │
│  15:05       300ml      │
│  13:40       250ml      │
│                         │
├─────────────────────────┤
│ 首页  统计  记录  设置   │
└─────────────────────────┘
```

---

# 七十九、V1.0 最终功能清单

### 必须实现

* [x] 手机端响应式
* [x] 首页
* [x] 今日饮水量
* [x] 今日次数
* [x] 每日目标
* [x] 进度条
* [x] 快捷饮水
* [x] 自定义饮水量
* [x] 添加记录
* [x] 修改记录
* [x] 删除记录
* [x] 今日记录
* [x] 历史记录
* [x] 7天统计
* [x] 30天统计
* [x] 图表
* [x] 达标率
* [x] 每日目标设置
* [x] 快捷按钮设置
* [x] 深色模式
* [x] 数据导出
* [x] 数据导入
* [x] 数据清空
* [x] IndexedDB
* [x] PWA Manifest
* [x] PWA Icon
* [x] Service Worker
* [x] 离线使用
* [x] 添加到主屏幕

### 可选

* [ ] 喝水提醒
* [ ] 系统通知
* [ ] 饮水类型

### V2.0

* [ ] 用户注册
* [ ] 登录
* [ ] 云端同步
* [ ] 多设备同步
* [ ] 云端备份
* [ ] Apple Health
* [ ] Google Health Connect
* [ ] 原生 App

---

# 八十、给 AI 编程工具的最终开发指令

开发 AI 必须遵循以下要求：

> 请严格按照本需求文档开发「喝水记录 PWA」。
>
> 不要擅自增加复杂功能。
>
> 第一阶段不要加入登录、注册、后端、支付、会员、广告。
>
> 数据使用 IndexedDB 本地保存。
>
> 软件必须 Mobile First。
>
> 首页必须以“快速记录一次喝水”为核心。
>
> 用户点击快捷饮水按钮后必须立即完成记录，不要弹出确认窗口。
>
> 支持自定义饮水量。
>
> 所有记录必须支持修改和删除。
>
> 所有统计数据必须从 WaterRecord 数据计算，不允许维护容易产生不一致的冗余统计数据。
>
> 必须处理日期切换和用户本地时区。
>
> 必须支持深色模式。
>
> 必须支持 PWA。
>
> 必须支持基本离线使用。
>
> 必须支持数据 JSON/CSV 导出。
>
> 必须支持 JSON 数据导入。
>
> 必须避免出现 NaN、undefined、Infinity 等异常显示。
>
> 必须考虑 iPhone 安全区域。
>
> 必须兼容主流 Android 和 iOS 手机浏览器。
>
> 开发过程中优先保证：
>
> **数据正确性 > 操作速度 > 手机体验 > UI美观 > 扩展功能。**
>
> 完成一个阶段后先确保当前阶段功能正常，再继续下一阶段。
>
> 不要一次性加入 V2.0 功能。
>
> 最终目标不是做一个复杂的健康管理平台，而是做一个：
>
> **打开 → 点一下 → 记录完成**
>
> 的极简喝水记录 PWA。

---

# 八十一、产品最终定位

整个产品可以用一句话概括：

> **“记录每一杯水，而不是管理整个人生。”**

核心操作：

**打开 → 点击 250ml → 完成。**

统计只是辅助功能。

首页永远应该把：

**“今天喝了多少”**

和

**“快速记录一杯水”**

放在最重要的位置。
